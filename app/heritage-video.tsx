"use client";
import {useState} from 'react';
import {Play,ExternalLink} from 'lucide-react';
import {placeContext} from './additional-heritage';
import type {Place} from './library';
export default function HeritageVideo({place}:{place:Place}){const [playing,setPlaying]=useState(false);const context=placeContext[place.id];const publisher=context?.videoCredit||'UNESCO / NHK';if(!context)return null;return <article className="heritage-film"><div className="film-player">{playing&&context.videoId?<iframe title={`${place.name} - ${publisher}`} src={`https://www.youtube-nocookie.com/embed/${context.videoId}?autoplay=1`} allow="autoplay; encrypted-media; picture-in-picture" allowFullScreen/>:<div className="film-cover"><img src={place.image} alt={place.alt}/>{context.videoId?<button onClick={()=>setPlaying(true)}><Play size={25}/> Watch heritage film</button>:<a href={context.videoSource} target="_blank" rel="noreferrer"><Play size={25}/> Browse heritage videos <ExternalLink size={15}/></a>}</div>}</div><div className="film-caption"><h3>{place.name}: real heritage footage</h3><p>{publisher}. Original publisher retains all rights.</p><a href={context.videoSource} target="_blank" rel="noreferrer">Watch at the source <ExternalLink size={15}/></a><p className="small">Playback uses YouTube after you press play. If embedding is unavailable, use the source link.</p></div></article>}
